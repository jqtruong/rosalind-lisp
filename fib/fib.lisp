;;;; fib.lisp

(in-package #:fib)

;;; "fib" goes here. Hacks and glory await!

(defvar *sample* '(5 3))

(defun num-rabbits-pairs (months pairs-per-litter)
  "Given: Positive integers `MONTHS' and `PAIRS-PER-LITTER'.

Return: The total number of rabbit pairs that will be present after `MONTHS'
months, if we begin with 1 pair and in each generation, every pair of
reproduction-age rabbits produces a litter of `PAIRS-PER-LITTER' rabbit
pairs (instead of only 1 pair).

Sample Output:
19"
  )
